export enum EventType {
  CLICK = "click",
  MOUSE_MOVE = "mouse_move",
  SCROLL = "scroll",
  DOM_LOAD = "dom_load",
  DOM_UNLOAD = "dom_unload",
}

export enum ScrollDirection {
  UP = "up",
  DOWN = "down",
}
